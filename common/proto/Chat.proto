syntax = "proto3";
package proto.vox;

message Chat
{
    enum Type
    {
        P2P = 0;
        // ?
        GROUP = 1;
        // ?
        CHANNEL = 2;
    }
    
    int32 chat_id = 1;
    
    Type type = 2;
    
    // Participants
    // ...
}
